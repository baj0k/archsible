- name: Mpv - Install packages
  become: yes
  package:
    name: 
      - mpv
      - yt-dlp
    state: present

- name: Mpv - Symlink configuration
  file:
    src: "{{ ansible_env.PWD }}/files/mpv"
    path: "~/.config/mpv"
    state: link
